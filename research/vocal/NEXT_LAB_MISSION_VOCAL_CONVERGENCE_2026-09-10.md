# Next Lab Mission — Vocal Convergence Before Perceptual Tuning

Date: 2026-09-10
Purpose: ready-to-use mission for Ultramonkeydog Lab

## LAB EXECUTION SHELL

USE ULTRAMONKEYDOG LAB FULLY.

Start from the Lab, not directly in Monkey's Ear.

Use the live Lab surfaces, project registry, project-brief, routing, capability discovery, research continuity, operating perspectives, executor selection, project canon, receipts, and current repo truth to determine the strongest way to execute this mission.

Do not manually roleplay named agents. Use the actual Lab mechanisms.

Let the Lab recover current Monkey's Ear truth, resolve the registered working repo/branch, absorb relevant prior research and failures, choose the strongest execution lane, use real operating perspectives/capabilities where useful, repair a Lab organ only if real dogfooding exposes a blocker, converge validated product work into the canonical project branch, and preserve genuinely reusable learning.

Product work outranks Lab gardening.

---

# ACTUAL PRODUCT MISSION

Converge the existing Monkey's Ear vocal module into a coherent, robust instrument core before broad listening/tuning. The vocal feature family set is frozen. Do not invent another vocal subsystem.

Read current repo truth first, including the newest perceptual/usability/robustness research and open-source design-mining notes. Treat live production code as stronger truth than optimistic receipts or design prose.

## Product intent

Monkey's Ear is not trying to become Nectar or a generic vocal channel strip. It is a programmable vocal pitch-expression instrument with a simple musical surface and optional deep/mad-scientist access.

Keep deep controls. Hard-to-understand controls are allowed when they expose a real sonic mechanism. Do not require deep controls for normal operation.

Do not overcomplicate the scale system. Keep degrees/cents/ratios, enable state, gravity, directionality, arbitrary period/non-octave behavior, and current trajectory/history behavior. Additional expert parameters are allowed only if they expose a proven useful mechanism; do not force a large degree-role ontology.

## Required convergence work

1. Inspect the actual current vocal production owner and identify which frozen features are implemented, partial, research-only, or absent.
2. Refactor only where it reduces collision/duplication or creates a clear shared semantic boundary. Avoid architecture theater.
3. Establish one bounded shared vocal-analysis representation or equivalent authoritative evidence path so target logic, transformation, dynamics, and saturation do not each invent contradictory analysis.
4. Correct semantic mistakes before adding sophistication. In particular, do not call generic fast-minus-slow pitch residual 'vibrato' unless periodic evidence supports it, and do not call scalar amplitude-envelope restoration true formant preservation.
5. Preserve/evolve causal PSOLA as the LIVE periodic transform. Make transformation authority source/confidence aware so uncertain, transient, or aperiodic material degrades toward protected/dry behavior unless explicit creative instability requests otherwise.
6. Keep pitch correction and intentional transposition independent but composable.
7. Implement/refine the existing formant/identity path at the strongest bounded level justified by current architecture. Use research comparisons to choose mechanisms; do not import incompatible-license code.
8. Implement/refine performance-aware dynamics using stable conventional gain-control foundations steered within bounded ranges by vocal evidence. Novelty comes from musical awareness, not unstable compressor math.
9. Implement/refine source/component-aware saturation. Benchmark antialias strategies where useful; do not silently accept latency that violates LIVE intent.
10. Establish explicit parameter ownership, sanitation, mapping, and transition behavior. Do not use one generic smoothing rule for every parameter.
11. Add hostile-input and lifecycle robustness around the mechanisms touched in this wave: silence, bad/non-finite external values, confidence collapse, automation jumps, reset, sample-rate/block changes, tuning edge cases, extreme legal deep-control combinations, and long decays/denormals where relevant.
12. Preserve output safety and hard RT rules: no audio-thread allocation/deallocation, locks, blocking, file/network I/O, logging, unbounded work, or unsafe dynamic topology mutation.

## Development/testing policy

We are still completing and integrating the required vocal core. Do cheap high-value proof continuously: compile/build, deterministic unit/math checks, bounds/finite checks, focused regression tests, RT contract inspection, and targeted synthetic fixtures.

Do NOT burn the wave polishing an unrelated flaky test harness or repeatedly demanding REAPER/hardware/listening proof while required architecture is still incomplete. Record/isolate unrelated failures and continue when product risk permits.

However, this wave must prepare the module for the later perceptual phase. Add the metrics/fixtures needed to sweep controls and compare variants without pretending those metrics prove sound quality.

## Perceptual/usability preparation

For important controls, separate normalized UI value from DSP value so later listening can shape nonlinear perceptual mappings. Support the conceptual regions subtle -> useful-natural -> useful-character -> extreme-interesting while keeping unsafe states unreachable.

Preserve three conceptual access levels without requiring final UI work now:

- PLAY: immediate musical macros/controls;
- ADVANCED: musician-facing mechanism controls;
- DEEP/LAB: real low-level mechanism controls.

Do not remove deep controls for simplicity. Make defaults and mappings carry the usability burden.

## Success condition

Finish with the vocal architecture materially more complete, coherent, bounded, and ready for perceptual tuning than it was at start. The end state should make the next major phase obvious: controlled listening/sweet-spot tuning and interaction polish, then vocal freeze and return to the synthesizer.

Do not claim naturalness, commercial superiority, physical latency, DAW reliability, or release readiness without corresponding evidence.

Converge validated changes to canonical project truth with a compact receipt describing actual code changed, tests actually run, unresolved blockers, and the exact next perceptual-tuning step.